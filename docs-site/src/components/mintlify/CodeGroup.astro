---
// Mintlify's <CodeGroup> tabs several code blocks under one header. Every one
// of the 111 uses in this corpus wraps exactly one block, so there is nothing
// to tab between and the wrapper renders its child straight through. If a page
// ever puts two blocks inside one, swap this for Starlight's <Tabs>.
---
<slot />
